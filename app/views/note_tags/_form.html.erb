<%= form_for(note_tag) do |f| %>
  <% if note_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note_tag.errors.count, "error") %> prohibited this note_tag from being saved:</h2>

      <ul>
      <% note_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note_id %>
    <%= f.text_field :note_id %>
  </div>

  <div class="field">
    <%= f.label :tag_id %>
    <%= f.text_field :tag_id %>
  </div>

  <div class="field">
    <%= f.label :added_by_id %>
    <%= f.text_field :added_by_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
